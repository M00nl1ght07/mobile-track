# 🗺️ GPS Tracker

GPS Tracker - это мобильное приложение для Android, которое позволяет пользователям отслеживать свои маршруты, измерять пройденное расстояние, скорость и время в пути. Приложение предоставляет удобный интерфейс для записи, сохранения и просмотра маршрутов на карте.

## 📌 Содержание

- [Функциональность](#функциональность)
- [Архитектура приложения](#архитектура-приложения)
- [Экраны приложения](#экраны-приложения)
  - [Главный экран](#главный-экран)
  - [Список маршрутов](#список-маршрутов)
  - [Просмотр маршрута](#просмотр-маршрута)
  - [Настройки](#настройки)
- [Технические детали](#технические-детали)
  - [Используемые библиотеки](#используемые-библиотеки)
  - [Структура проекта](#структура-проекта)
  - [База данных](#база-данных)
- [Установка и запуск](#установка-и-запуск)
- [🎨 Пользовательский интерфейс и взаимодействие](#пользовательский-интерфейс-и-взаимодействие)
  - [Дизайн приложения](#дизайн-приложения)
  - [Подробное описание экранов](#подробное-описание-экранов)
    - [Главный экран (MainFragment)](#главный-экран-mainfragment)
    - [Экран списка маршрутов (TrackListFragment)](#экран-списка-маршрутов-tracklistfragment)
    - [Экран просмотра маршрута (ViewTrackFragment)](#экран-просмотра-маршрута-viewtrackfragment)
    - [Экран настроек (SettingsFragment)](#экран-настроек-settingsfragment)
  - [Диалоговые окна](#диалоговые-окна)
  - [Иконки и графические ресурсы](#иконки-и-графические-ресурсы)
  - [Стили и темы](#стили-и-темы)
  - [Локализация](#локализация)

## 🌟 Функциональность

- **Отслеживание маршрута**: запись пути с использованием GPS
- **Измерение параметров**: расстояние, время, текущая и средняя скорость
- **Сохранение маршрутов**: возможность сохранять пройденные маршруты
- **История маршрутов**: просмотр списка сохраненных маршрутов
- **Детальный просмотр**: возможность просмотреть любой сохраненный маршрут на карте
- **Настройки приложения**: настройка параметров отслеживания

## 🏗️ Архитектура приложения

Приложение разработано с использованием современных подходов к разработке Android-приложений:

- **Архитектурный паттерн**: MVVM (Model-View-ViewModel)
- **Компоненты**: Fragments, ViewModel, LiveData, Room
- **Навигация**: BottomNavigationView для переключения между основными экранами
- **Хранение данных**: Room Database для сохранения маршрутов
- **Карты**: OSMDroid для отображения карт и маршрутов

## 📱 Экраны приложения

### Главный экран

Главный экран приложения (MainFragment) отображает карту и панель с информацией о текущем маршруте:

- **Карта**: отображает текущее местоположение пользователя и записываемый маршрут
- **Информационная панель**: показывает время в пути, среднюю скорость, текущую скорость и пройденное расстояние
- **Кнопка старт/стоп**: позволяет начать или остановить запись маршрута
- **Кнопка определения местоположения**: центрирует карту на текущем местоположении пользователя

**Технические детали**:
- Файл макета: `fragment_main.xml`
- Класс фрагмента: `MainFragment.kt`
- Используемые компоненты: OSMDroid MapView, CardView, FloatingActionButton

### Список маршрутов

Экран списка маршрутов (TrackListFragment) отображает все сохраненные маршруты:

- **Список маршрутов**: отображает дату, время, расстояние и среднюю скорость для каждого маршрута
- **Удаление маршрута**: возможность удалить маршрут из списка
- **Переход к просмотру**: при нажатии на маршрут открывается экран детального просмотра

**Технические детали**:
- Файл макета: `fragment_track_list.xml`
- Макет элемента списка: `track_item.xml`
- Класс фрагмента: `TrackListFragment.kt`
- Адаптер: `TrackAdapter.kt`
- Используемые компоненты: RecyclerView, CardView

### Просмотр маршрута

Экран просмотра маршрута (ViewTrackFragment) позволяет детально просмотреть сохраненный маршрут:

- **Карта**: отображает полный маршрут на карте
- **Информационная панель**: показывает дату, время, среднюю скорость и пройденное расстояние
- **Кнопка центрирования**: центрирует карту на маршруте

**Технические детали**:
- Файл макета: `fragment_view_track.xml`
- Класс фрагмента: `ViewTrackFragment.kt`
- Используемые компоненты: OSMDroid MapView, CardView, FloatingActionButton

### Настройки

Экран настроек (SettingsFragment) позволяет настроить параметры приложения:

- **Настройки отслеживания**: частота обновления местоположения, минимальное расстояние для обновления и т.д.
- **Настройки отображения**: единицы измерения, стиль карты

**Технические детали**:
- Файл макета: `fragment_settings.xml`
- Класс фрагмента: `SettingsFragment.kt`
- Используемые компоненты: PreferenceFragmentCompat

## ⚙️ Технические детали  

### Используемые библиотеки и технологии

- **OSMDroid**: Библиотека для отображения карт OpenStreetMap
  - Используется для визуализации карты и маршрутов
  - Поддерживает отображение маркеров, линий и полигонов
  - Файлы: `MapView` в `fragment_main.xml` и `fragment_view_track.xml`

- **Room Database**: Библиотека для работы с SQLite
  - Обеспечивает абстракцию над SQLite для удобной работы с базой данных
  - Используется для хранения информации о маршрутах и точках маршрутов
  - Основные файлы: `MainDb.kt`, `TrackDao.kt`, `TrackItem.kt`, `TrackLocationItem.kt`

- **ViewModel и LiveData**: Компоненты архитектуры Android
  - ViewModel отделяет данные UI от логики и обеспечивает сохранение данных при изменении конфигурации
  - LiveData обеспечивает реактивное обновление UI при изменении данных
  - Файлы: `MainViewModel.kt`, `TrackViewModel.kt`

- **Material Components**: Библиотека UI-компонентов от Google
  - Обеспечивает современный внешний вид приложения
  - Используются компоненты: CardView, FloatingActionButton, BottomNavigationView
  - Файлы: все XML-файлы макетов в `res/layout`

- **LocationServices**: API для работы с местоположением
  - Используется для получения текущего местоположения пользователя
  - Обеспечивает отслеживание перемещений в реальном времени
  - Файлы: `LocationService.kt`, `LocationModel.kt`

- **Coroutines**: Библиотека для асинхронного программирования
  - Используется для выполнения операций с базой данных в фоновом потоке
  - Обеспечивает неблокирующие операции ввода-вывода
  - Применяется в ViewModel и Repository классах

### Подробная структура проекта

#### Пакет `fragments`

Содержит все фрагменты приложения, каждый из которых отвечает за отдельный экран:

- **MainFragment.kt**: Главный экран с картой и информацией о текущем маршруте
  - Отвечает за отображение карты и текущего маршрута
  - Обрабатывает взаимодействие пользователя с картой
  - Отображает информацию о текущем маршруте (время, расстояние, скорость)
  - Связан с макетом `fragment_main.xml`

- **TrackListFragment.kt**: Экран со списком сохраненных маршрутов
  - Отображает список всех сохраненных маршрутов
  - Обрабатывает нажатия на элементы списка для перехода к просмотру маршрута
  - Обрабатывает удаление маршрутов
  - Связан с макетом `fragment_track_list.xml`

- **ViewTrackFragment.kt**: Экран просмотра сохраненного маршрута
  - Отображает выбранный маршрут на карте
  - Показывает информацию о маршруте (дата, время, расстояние, скорость)
  - Связан с макетом `fragment_view_track.xml`

- **SettingsFragment.kt**: Экран настроек приложения
  - Позволяет пользователю настраивать параметры отслеживания
  - Связан с макетом `fragment_settings.xml`

#### Пакет `database`

Содержит классы для работы с базой данных:

- **MainDb.kt**: Основной класс базы данных
  - Определяет структуру базы данных
  - Создает и обновляет таблицы
  - Предоставляет доступ к DAO (Data Access Object)

- **TrackDao.kt**: Интерфейс для доступа к данным
  - Содержит методы для выполнения операций CRUD (Create, Read, Update, Delete)
  - Использует аннотации Room для определения SQL-запросов
  - Методы: `insertTrack()`, `getAllTracks()`, `deleteTrack()` и др.

- **TrackItem.kt**: Сущность, представляющая маршрут
  - Содержит информацию о маршруте: id, дата, время, расстояние, скорость
  - Аннотирована как `@Entity` для использования с Room

- **TrackLocationItem.kt**: Сущность, представляющая точки маршрута
  - Содержит информацию о точке: id, trackId, широта, долгота, время
  - Аннотирована как `@Entity` для использования с Room

#### Пакет `location`

Содержит классы для работы с местоположением:

- **LocationService.kt**: Сервис для отслеживания местоположения
  - Работает в фоновом режиме для отслеживания перемещений пользователя
  - Использует FusedLocationProviderClient для получения данных о местоположении
  - Отправляет обновления местоположения через LiveData

- **LocationModel.kt**: Модель данных местоположения
  - Содержит методы для работы с данными о местоположении
  - Вычисляет расстояние, скорость и другие параметры

#### Пакет `utils`

Содержит вспомогательные классы и утилиты:

- **DialogManager.kt**: Управление диалоговыми окнами
  - Создает и отображает диалоговые окна для сохранения маршрута и других действий
  - Связан с макетом `save_track_dialog.xml`

- **TimeUtils.kt**: Утилиты для работы со временем
  - Содержит методы для форматирования времени и даты
  - Используется для отображения времени в пути и даты маршрута

- **Extensions.kt**: Расширения Kotlin
  - Содержит расширения для стандартных классов Kotlin и Android
  - Упрощает часто используемые операции

#### Пакет `main`

Содержит основную активность и классы для навигации:

- **MainActivity.kt**: Основная активность приложения
  - Содержит BottomNavigationView для навигации между фрагментами
  - Управляет жизненным циклом приложения
  - Связана с макетом `activity_main.xml`

- **MainViewModel.kt**: ViewModel для основной активности
  - Хранит и управляет данными UI
  - Обеспечивает сохранение данных при изменении конфигурации
  - Взаимодействует с репозиториями для получения данных

### Подробное описание базы данных

Приложение использует Room Database для хранения информации о маршрутах. База данных состоит из двух основных таблиц:

#### Таблица `track_table`

Хранит информацию о маршрутах:

- **id**: Уникальный идентификатор маршрута (первичный ключ)
- **date**: Дата создания маршрута
- **time**: Время в пути
- **distance**: Пройденное расстояние
- **velocity**: Средняя скорость
- **image**: Изображение маршрута (опционально)

#### Таблица `track_location_table`

Хранит информацию о точках маршрута:

- **id**: Уникальный идентификатор точки (первичный ключ)
- **trackId**: Идентификатор маршрута (внешний ключ)
- **latitude**: Широта
- **longitude**: Долгота
- **time**: Время фиксации точки

### Процесс отслеживания маршрута

1. **Запуск отслеживания**:
   - Пользователь нажимает кнопку "Старт" на главном экране
   - Приложение запускает LocationService для отслеживания местоположения
   - Начинается запись времени в пути

2. **Обновление данных**:
   - LocationService получает обновления местоположения
   - Новые точки добавляются в список точек маршрута
   - Обновляется информация о расстоянии, скорости и времени
   - UI обновляется с новыми данными через LiveData

3. **Остановка отслеживания**:
   - Пользователь нажимает кнопку "Стоп"
   - Приложение останавливает LocationService
   - Отображается диалог сохранения маршрута

4. **Сохранение маршрута**:
   - Пользователь подтверждает сохранение
   - Данные о маршруте и точках сохраняются в базу данных
   - Приложение возвращается в исходное состояние

### Отображение маршрута на карте

Для отображения маршрута на карте используется библиотека OSMDroid:

1. **Инициализация карты**:
   - Создается экземпляр MapView
   - Устанавливаются начальные параметры (масштаб, центр)
   - Добавляются слои для отображения маркеров и линий

2. **Отображение текущего местоположения**:
   - На карту добавляется маркер текущего местоположения
   - Маркер обновляется при получении новых данных о местоположении

3. **Отображение маршрута**:
   - Точки маршрута соединяются линиями
   - Линии отображаются на карте в виде полилинии
   - Цвет и толщина линии настраиваются

4. **Взаимодействие с картой**:
   - Пользователь может перемещать и масштабировать карту
   - Кнопка центрирования возвращает карту к текущему местоположению

## 💻 Установка и запуск

1. Клонируйте репозиторий
2. Откройте проект в Android Studio
3. Подключите устройство Android или запустите эмулятор
4. Запустите приложение

## 🎨 Пользовательский интерфейс и взаимодействие

### Дизайн приложения

Приложение использует современный Material Design с акцентом на удобство использования:

- **Цветовая схема**: Основана на зеленых оттенках (colorPrimary, colorAccent), символизирующих природу и активность
- **Типографика**: Используется системный шрифт для обеспечения согласованности с остальной частью системы
- **Иконки**: Векторные иконки из библиотеки Material Design Icons, обеспечивающие четкость при любом разрешении экрана
- **Анимации**: Плавные переходы между экранами и состояниями для улучшения пользовательского опыта

### Подробное описание экранов

#### Главный экран (MainFragment)

Главный экран является центральным элементом приложения и содержит следующие компоненты:

1. **Карта (MapView)**:
   - Занимает большую часть экрана
   - Отображает текущее местоположение пользователя с помощью маркера
   - Показывает записываемый маршрут в виде линии
   - Поддерживает жесты масштабирования и перемещения

2. **Информационная панель (CardView)**:
   - Расположена в верхней части экрана
   - Содержит следующую информацию:
     - **Время в пути**: Отображается в формате "ЧЧ:ММ:СС"
     - **Средняя скорость**: В км/ч
     - **Текущая скорость**: В км/ч
     - **Пройденное расстояние**: В метрах или километрах
   - Имеет закругленные углы и тень для визуального выделения

3. **Кнопка старт/стоп (FloatingActionButton)**:
   - Расположена в правом нижнем углу экрана
   - Меняет свой вид в зависимости от состояния:
     - Зеленая с иконкой "play" (`ic_start_track.xml`) в состоянии ожидания
     - Красная с иконкой "stop" (`ic_stop_track.xml`) во время записи маршрута
   - При нажатии запускает или останавливает запись маршрута

4. **Кнопка определения местоположения (FloatingActionButton)**:
   - Расположена под кнопкой старт/стоп
   - Имеет белый фон с зеленой иконкой местоположения (`ic_my_location.xml`)
   - При нажатии центрирует карту на текущем местоположении пользователя

**Взаимодействие с пользователем**:
- При первом запуске приложение запрашивает разрешение на доступ к местоположению
- Если GPS отключен, приложение предлагает включить его через диалоговое окно
- Пользователь может начать запись маршрута, нажав на кнопку "Старт"
- Во время записи информационная панель обновляется в реальном времени
- Пользователь может остановить запись, нажав на кнопку "Стоп"
- После остановки записи появляется диалог с предложением сохранить маршрут

**Технические особенности реализации**:
- Карта инициализируется в методе `onViewCreated()` фрагмента
- Для отслеживания местоположения используется `LocationService`
- Обновление UI происходит через `LiveData` в `MainViewModel`
- Запись маршрута реализована с помощью сервиса, работающего в фоновом режиме

Ключевые свойства:
- `binding`: ViewBinding для доступа к элементам UI
- `mMap`: Экземпляр MapView для отображения карты
- `mainViewModel`: ViewModel для управления данными
- `polyline`: Линия для отображения маршрута на карте

Ключевые методы:
- `onCreateView()`: Создает и возвращает корневой View фрагмента
- `onViewCreated()`: Инициализирует компоненты после создания View
- `initMap()`: Настраивает карту (источник тайлов, масштаб, контроллеры)
- `initObservers()`: Устанавливает наблюдателей для LiveData из ViewModel
- `initButtons()`: Настраивает обработчики нажатий для кнопок
- `startTrack()`, `stopTrack()`: Управляют записью маршрута
- `updatePolyline()`: Обновляет линию маршрута на карте
- `updateMapLocation()`: Обновляет отображение текущего местоположения на карте
- `checkLocationPermission()`: Проверяет и запрашивает разрешение на доступ к местоположению

#### Экран списка маршрутов (TrackListFragment)

Экран списка маршрутов отображает все сохраненные маршруты и предоставляет возможность управления ими:

1. **Список маршрутов (RecyclerView)**:
   - Занимает большую часть экрана
   - Каждый элемент списка представляет собой карточку (CardView) с информацией о маршруте:
     - Дата и время создания
     - Продолжительность маршрута
     - Пройденное расстояние
     - Средняя скорость
   - Элементы списка отсортированы по дате (новые сверху)

2. **Пустое состояние**:
   - Отображается, когда список маршрутов пуст
   - Содержит текст с предложением создать первый маршрут

**Взаимодействие с пользователем**:
- Пользователь может прокручивать список для просмотра всех маршрутов
- При нажатии на элемент списка открывается экран просмотра маршрута
- Для удаления маршрута пользователь может выполнить свайп влево или вправо
- Перед удалением появляется диалог подтверждения

**Технические особенности реализации**:
- Список реализован с помощью `RecyclerView` и адаптера `TrackAdapter`
- Данные загружаются из базы данных через `TrackViewModel`
- Для обработки свайпов используется `ItemTouchHelper`
- Переход к просмотру маршрута реализован через передачу ID маршрута в аргументах фрагмента

#### Экран просмотра маршрута (ViewTrackFragment)

Экран просмотра маршрута позволяет детально изучить сохраненный маршрут:

1. **Карта (MapView)**:
   - Занимает большую часть экрана
   - Отображает полный маршрут в виде линии
   - Показывает начальную и конечную точки маршрута с помощью маркеров
   - Автоматически масштабируется для отображения всего маршрута

2. **Информационная панель (CardView)**:
   - Расположена в верхней части экрана
   - Содержит следующую информацию:
     - **Дата**: Дата создания маршрута
     - **Время**: Продолжительность маршрута
     - **Средняя скорость**: В км/ч
     - **Пройденное расстояние**: В метрах или километрах
   - Имеет закругленные углы и тень для визуального выделения

3. **Кнопка центрирования (FloatingActionButton)**:
   - Расположена в правом нижнем углу экрана
   - При нажатии центрирует карту на маршруте

**Взаимодействие с пользователем**:
- Пользователь может масштабировать и перемещать карту для детального изучения маршрута
- Кнопка центрирования позволяет быстро вернуться к обзору всего маршрута
- Для возврата к списку маршрутов пользователь может нажать кнопку "Назад"

**Технические особенности реализации**:
- Маршрут загружается из базы данных по ID, переданному в аргументах фрагмента
- Для отображения маршрута используется `Polyline` из библиотеки OSMDroid
- Информация о маршруте форматируется с помощью утилитных методов

#### Экран настроек (SettingsFragment)

Экран настроек позволяет пользователю настроить параметры приложения:

1. **Настройки отслеживания**:
   - **Частота обновления**: Определяет, как часто приложение запрашивает обновление местоположения
   - **Минимальное расстояние**: Минимальное расстояние (в метрах) для обновления местоположения
   - **Точность**: Выбор между высокой точностью (с использованием GPS) и экономией батареи

2. **Настройки отображения**:
   - **Единицы измерения**: Выбор между метрической (км) и имперской (мили) системами
   - **Тема**: Выбор между светлой и темной темами (если поддерживается)
   - **Стиль карты**: Выбор различных стилей отображения карты

**Взаимодействие с пользователем**:
- Пользователь может изменять настройки, нажимая на соответствующие элементы
- Изменения сохраняются автоматически
- Некоторые настройки применяются немедленно, другие - при следующем запуске приложения

**Технические особенности реализации**:
- Настройки реализованы с помощью `PreferenceFragmentCompat`
- Значения настроек хранятся в `SharedPreferences`
- Для доступа к настройкам из других частей приложения используется класс `PreferenceManager`

### Диалоговые окна

Приложение использует несколько типов диалоговых окон для взаимодействия с пользователем:

1. **Диалог сохранения маршрута**:
   - Отображается после остановки записи маршрута
   - Содержит информацию о маршруте (время, расстояние, скорость)
   - Имеет две кнопки: "Сохранить" и "Отмена"
   - Реализован в классе `DialogManager.kt` с использованием макета `save_track_dialog.xml`

2. **Диалог включения GPS**:
   - Отображается, если GPS отключен
   - Предлагает пользователю включить GPS
   - Имеет две кнопки: "Да" и "Нет"
   - Реализован в классе `DialogManager.kt`

3. **Диалог подтверждения удаления**:
   - Отображается перед удалением маршрута
   - Запрашивает подтверждение действия
   - Имеет две кнопки: "Удалить" и "Отмена"
   - Реализован с помощью стандартного `AlertDialog`

### Иконки и графические ресурсы

Приложение использует набор векторных иконок для различных элементов интерфейса:

1. **Иконки навигации**:
   - `ic_map.xml`: Иконка карты для нижней панели навигации
   - `ic_history.xml`: Иконка истории для нижней панели навигации
   - `ic_settings.xml`: Иконка настроек для нижней панели навигации

2. **Иконки действий**:
   - `ic_start_track.xml`: Иконка запуска записи маршрута
   - `ic_stop_track.xml`: Иконка остановки записи маршрута
   - `ic_my_location.xml`: Иконка определения местоположения
   - `ic_delete.xml`: Иконка удаления маршрута

3. **Информационные иконки**:
   - `ic_time_refresh.xml`: Иконка времени для информационной панели
   - `ic_speed.xml`: Иконка скорости для информационной панели
   - `ic_distance.xml`: Иконка расстояния для информационной панели

4. **Фоновые ресурсы**:
   - `bottom_nav_background.xml`: Фон для нижней панели навигации с закругленными углами
   - `bottom_nav_colors.xml`: Цвета для элементов нижней панели навигации

### Стили и темы

Приложение использует систему стилей и тем Android для обеспечения единообразного внешнего вида:

1. **Основная тема**:
   - Определена в `styles.xml`
   - Устанавливает основные цвета приложения (colorPrimary, colorAccent)
   - Определяет стиль панели действий и статус-бара

2. **Стили компонентов**:
   - Стили для кнопок, текстовых полей и других элементов интерфейса
   - Обеспечивают единообразие внешнего вида по всему приложению

3. **Цветовые ресурсы**:
   - Определены в `colors.xml`
   - Включают основные цвета приложения и их оттенки
   - Используются для различных элементов интерфейса

### Локализация

Приложение поддерживает локализацию для различных языков:

1. **Строковые ресурсы**:
   - Основные строки определены в `strings.xml`
   - Переводы для других языков находятся в соответствующих папках (`values-ru`, `values-de` и т.д.)

2. **Форматирование**:
   - Учитываются особенности форматирования чисел и дат для разных локалей
   - Используются соответствующие методы форматирования из Android API

## 💻 Подробное описание кода и реализации

### Основные классы и их функциональность

#### MainActivity.kt

Основная активность приложения, которая служит контейнером для всех фрагментов:
- Инициализирует нижнюю панель навигации (BottomNavigationView)
- Обрабатывает переключение между фрагментами (MainFragment, TrackListFragment, SettingsFragment)
- Управляет жизненным циклом приложения
- Использует ViewBinding для доступа к элементам интерфейса

Ключевые методы:
- `onCreate()`: Инициализирует активность, настраивает BottomNavigationView
- `setBottomNavListener()`: Устанавливает слушатель для обработки нажатий на элементы нижней навигации
- `openFragment()`: Заменяет текущий фрагмент на новый в контейнере

#### MainViewModel.kt

ViewModel для главного экрана, который управляет данными и состоянием UI:
- Управляет данными о местоположении через LocationService
- Отслеживает время, расстояние и скорость
- Обновляет UI через LiveData
- Управляет записью маршрута (старт/стоп)
- Хранит список точек маршрута в процессе записи

Ключевые свойства:
- `locationUpdates`: LiveData с обновлениями местоположения
- `timeData`, `distanceData`, `velocityData`, `averageVelocityData`: LiveData для отображения информации
- `trackPoints`: Список точек текущего маршрута
- `isTrackStarted`: Флаг, указывающий, идет ли запись маршрута

Ключевые методы:
- `startTrack()`: Начинает запись маршрута, запускает таймер
- `stopTrack()`: Останавливает запись маршрута, останавливает таймер
- `updateTime()`, `updateDistance()`, `updateVelocity()`: Обновляют соответствующие данные

#### LocationService.kt

Сервис для отслеживания местоположения пользователя:
- Использует FusedLocationProviderClient для получения данных о местоположении
- Настраивает параметры запроса местоположения (частота, точность)
- Отправляет обновления местоположения через LiveData
- Работает в фоновом режиме для непрерывного отслеживания

Ключевые свойства:
- `_locationUpdates`: MutableLiveData для хранения последнего местоположения
- `fusedLocationClient`: Клиент для работы с API местоположения
- `locationCallback`: Обратный вызов для получения обновлений местоположения

Ключевые методы:
- `startLocationUpdates()`: Начинает отслеживание местоположения
- `stopLocationUpdates()`: Останавливает отслеживание местоположения
- `onLocationResult()`: Обрабатывает полученные данные о местоположении

#### MainFragment.kt

Главный фрагмент приложения, отображающий карту и информацию о текущем маршруте:
- Инициализирует и настраивает карту OSMDroid
- Обрабатывает обновления местоположения и отображает их на карте
- Управляет UI для отображения информации о маршруте
- Обрабатывает нажатия на кнопки для начала/остановки записи маршрута
- Отображает маршрут на карте в виде линии (Polyline)

Ключевые свойства:
- `binding`: ViewBinding для доступа к элементам UI
- `mMap`: Экземпляр MapView для отображения карты
- `mainViewModel`: ViewModel для управления данными
- `polyline`: Линия для отображения маршрута на карте

Ключевые методы:
- `onCreateView()`: Создает и возвращает корневой View фрагмента
- `onViewCreated()`: Инициализирует компоненты после создания View
- `initMap()`: Настраивает карту (источник тайлов, масштаб, контроллеры)
- `initObservers()`: Устанавливает наблюдателей для LiveData из ViewModel
- `initButtons()`: Настраивает обработчики нажатий для кнопок
- `startTrack()`, `stopTrack()`: Управляют записью маршрута
- `updatePolyline()`: Обновляет линию маршрута на карте
- `updateMapLocation()`: Обновляет отображение текущего местоположения на карте
- `checkLocationPermission()`: Проверяет и запрашивает разрешение на доступ к местоположению

#### TrackListFragment.kt

Фрагмент для отображения списка сохраненных маршрутов:
- Инициализирует RecyclerView для отображения списка маршрутов
- Обрабатывает нажатия на элементы списка для перехода к просмотру маршрута
- Обрабатывает свайпы для удаления маршрутов
- Отображает пустое состояние, когда список маршрутов пуст
- Использует TrackAdapter для связывания данных с UI

#### ViewTrackFragment.kt

Фрагмент для просмотра сохраненного маршрута:
- Загружает информацию о маршруте из базы данных по ID
- Отображает маршрут на карте в виде линии
- Отображает информацию о маршруте (дата, время, расстояние, скорость)
- Обрабатывает нажатие на кнопку центрирования для отображения всего маршрута
- Получает данные через TrackViewModel

#### TrackViewModel.kt

ViewModel для работы с маршрутами:
- Предоставляет доступ к данным о маршрутах через DAO
- Выполняет операции с базой данных в фоновом потоке с помощью Coroutines
- Обеспечивает реактивное обновление UI через LiveData
- Содержит методы для вставки, удаления и получения маршрутов

#### DialogManager.kt

Класс для управления диалоговыми окнами в приложении:
- Создает и отображает различные типы диалогов
- Обрабатывает взаимодействие пользователя с диалогами
- Использует паттерн "Одиночка" (Singleton) для доступа к методам

Ключевые методы:
- `showSaveTrackDialog()`: Отображает диалог сохранения маршрута
- `showLocationEnableDialog()`: Отображает диалог включения GPS
- `showDeleteTrackDialog()`: Отображает диалог подтверждения удаления маршрута

Интерфейсы:
- `Listener`: Интерфейс для обратных вызовов при взаимодействии с диалогами

#### TrackAdapter.kt

Адаптер для отображения списка маршрутов в RecyclerView:
- Связывает данные маршрутов с элементами пользовательского интерфейса
- Обрабатывает нажатия на элементы списка
- Использует DiffUtil для эффективного обновления списка

Ключевые компоненты:
- `ViewHolder`: Внутренний класс, хранящий ссылки на элементы UI для одного элемента списка
- `Listener`: Интерфейс для обработки нажатий на элементы списка
- `DiffCallback`: Класс для сравнения элементов списка при обновлении

Ключевые методы:
- `onCreateViewHolder()`: Создает новый ViewHolder
- `onBindViewHolder()`: Связывает данные с ViewHolder
- `submitList()`: Обновляет список элементов с использованием DiffUtil

#### DialogManager.kt

Класс для управления диалоговыми окнами в приложении:
- Создает и отображает различные типы диалогов
- Обрабатывает взаимодействие пользователя с диалогами
- Использует паттерн "Одиночка" (Singleton) для доступа к методам

Ключевые методы:
- `showSaveTrackDialog()`: Отображает диалог сохранения маршрута
- `showLocationEnableDialog()`: Отображает диалог включения GPS
- `showDeleteTrackDialog()`: Отображает диалог подтверждения удаления маршрута

Интерфейсы:
- `Listener`: Интерфейс для обратных вызовов при взаимодействии с диалогами

Класс данных для представления точки маршрута в процессе записи:
- Хранит координаты точки (широта, долгота)
- Хранит время фиксации точки
- Используется для временного хранения точек до сохранения маршрута

Свойства:
- `latitude`: Широта точки
- `longitude`: Долгота точки
- `time`: Время фиксации точки в миллисекундах

### База данных и модели данных

#### MainDb.kt

Основной класс базы данных:
- Определяет структуру базы данных
- Создает и обновляет таблицы
- Предоставляет доступ к DAO (Data Access Object)

#### TrackDao.kt

Интерфейс для доступа к данным:
- Определяет методы для выполнения операций CRUD с базой данных
- Использует аннотации Room для определения SQL-запросов
- Возвращает результаты в виде LiveData для реактивного обновления UI
- Содержит методы для работы с маршрутами и точками маршрутов

#### TrackItem.kt

Сущность, представляющая маршрут:
- Представляет запись в таблице track_table
- Содержит информацию о маршруте (id, дата, время, расстояние, скорость)
- Имеет автоматически генерируемый первичный ключ
- Аннотирована как @Entity для использования с Room

#### TrackLocationItem.kt

Сущность, представляющая точки маршрута:
- Представляет запись в таблице track_location_table
- Содержит информацию о точке маршрута (id, trackId, широта, долгота, время)
- Связана с маршрутом через поле trackId
- Аннотирована как @Entity для использования с Room

### Утилиты и вспомогательные классы

#### TimeUtils.kt

Утилитарный класс для работы со временем и датами:
- Предоставляет статические методы для форматирования времени
- Используется для отображения времени в пути и даты маршрута
- Обеспечивает единый формат времени во всем приложении

Ключевые методы:
- `formatTime()`: Преобразует миллисекунды в формат "ЧЧ:ММ:СС"
- `getDate()`: Возвращает текущую дату в формате "дд/мм/гггг ЧЧ:мм"

#### TrackPoint.kt

Класс данных для представления точки маршрута в процессе записи:
- Хранит координаты точки (широта, долгота)
- Хранит время фиксации точки
- Используется для временного хранения точек до сохранения маршрута

Свойства:
- `latitude`: Широта точки
- `longitude`: Долгота точки
- `time`: Время фиксации точки в миллисекундах

### Ресурсы приложения

#### Макеты (layouts)

Приложение использует следующие макеты:

1. **activity_main.xml**: Основной макет активности
   - Содержит ConstraintLayout с плейсхолдером для фрагментов
   - Включает BottomNavigationView для навигации

2. **fragment_main.xml**: Макет главного экрана
   - Содержит MapView для отображения карты
   - Включает CardView с информацией о маршруте
   - Содержит FloatingActionButton для управления записью маршрута

3. **fragment_track_list.xml**: Макет экрана списка маршрутов
   - Содержит RecyclerView для отображения списка маршрутов
   - Включает TextView для отображения пустого состояния

4. **fragment_view_track.xml**: Макет экрана просмотра маршрута
   - Содержит MapView для отображения маршрута
   - Включает CardView с информацией о маршруте
   - Содержит FloatingActionButton для центрирования карты

5. **track_item.xml**: Макет элемента списка маршрутов
   - Содержит CardView с информацией о маршруте
   - Отображает дату, время, расстояние и скорость

6. **save_track_dialog.xml**: Макет диалога сохранения маршрута
   - Содержит информацию о маршруте
   - Включает кнопки "Сохранить" и "Отмена"

#### Строковые ресурсы (strings.xml)

Приложение использует строковые ресурсы для всех текстовых элементов интерфейса, включая:
- Названия экранов и вкладок
- Тексты кнопок и диалогов
- Сообщения об ошибках и информационные сообщения
- Подписи к элементам интерфейса

#### Цветовые ресурсы (colors.xml)

Приложение использует следующие основные цвета:
- colorPrimary: #4CAF50 (зеленый)
- colorPrimaryDark: #388E3C (темно-зеленый)
- colorAccent: #FF9800 (оранжевый)
- text_primary: #212121 (почти черный)
- text_secondary: #757575 (серый)
- track_line: #FF0000 (красный) - для отображения линии маршрута

### Разрешения и манифест

Приложение требует следующие разрешения, указанные в AndroidManifest.xml:
- ACCESS_FINE_LOCATION: для точного определения местоположения
- ACCESS_COARSE_LOCATION: для приблизительного определения местоположения
- INTERNET: для загрузки карт
- ACCESS_NETWORK_STATE: для проверки состояния сети
- WRITE_EXTERNAL_STORAGE: для сохранения данных
- FOREGROUND_SERVICE: для работы сервиса отслеживания в фоновом режиме

## 🔍 Алгоритмы и логика работы

### Алгоритм отслеживания маршрута

1. **Инициализация**:
   - Пользователь нажимает кнопку "Старт"
   - Приложение проверяет разрешения на доступ к местоположению
   - Если GPS отключен, предлагается включить его

2. **Запуск отслеживания**:
   - Запускается LocationService для получения обновлений местоположения
   - Инициализируется таймер для отслеживания времени
   - Создается пустой список точек маршрута

3. **Обработка обновлений местоположения**:
   - При получении нового местоположения оно добавляется в список точек
   - Рассчитывается пройденное расстояние между точками
   - Обновляется информация о скорости и среднем темпе
   - Обновляется UI с новыми данными
   - Точка добавляется на карту в виде части линии маршрута

4. **Остановка отслеживания**:
   - Пользователь нажимает кнопку "Стоп"
   - Останавливается LocationService
   - Останавливается таймер
   - Отображается диалог с предложением сохранить маршрут

5. **Сохранение маршрута**:
   - Если пользователь подтверждает сохранение, создается объект TrackItem
   - TrackItem и список точек сохраняются в базу данных
   - UI сбрасывается в исходное состояние

### Алгоритм расчета расстояния

```kotlin
fun calculateDistance(lat1: Double, lon1: Double, lat2: Double, lon2: Double): Float {
    val results = FloatArray(1)
    Location.distanceBetween(lat1, lon1, lat2, lon2, results)
    return results[0]
}

fun updateTotalDistance(newPoint: TrackPoint) {
    if (trackPoints.isNotEmpty()) {
        val lastPoint = trackPoints.last()
        val distance = calculateDistance(
            lastPoint.latitude, lastPoint.longitude,
            newPoint.latitude, newPoint.longitude
        )
        totalDistance += distance
        distanceData.postValue(formatDistance(totalDistance))
    }
}

fun formatDistance(distanceInMeters: Float): String {
    return if (distanceInMeters < 1000) {
        "${distanceInMeters.toInt()} м"
    } else {
        String.format("%.2f км", distanceInMeters / 1000)
    }
}
```

#### Алгоритм расчета скорости (подробно)

```kotlin
fun calculateCurrentSpeed(newPoint: TrackPoint): Float {
    // Берем последние несколько точек для более стабильного расчета
    val pointsForCalculation = trackPoints.takeLast(5).plus(newPoint)
    if (pointsForCalculation.size < 2) return 0f
    
    val firstPoint = pointsForCalculation.first()
    val lastPoint = pointsForCalculation.last()
    
    val distance = calculateDistance(
        firstPoint.latitude, firstPoint.longitude,
        lastPoint.latitude, lastPoint.longitude
    )
    
    val timeInSeconds = (lastPoint.time - firstPoint.time) / 1000f
    if (timeInSeconds <= 0) return 0f
    
    // Скорость в м/с
    val speedInMs = distance / timeInSeconds
    // Конвертация в км/ч
    return speedInMs * 3.6f
}

fun calculateAverageSpeed(): Float {
    if (trackPoints.size < 2 || totalDistance <= 0) return 0f
    
    val firstPoint = trackPoints.first()
    val lastPoint = trackPoints.last()
    
    val timeInSeconds = (lastPoint.time - firstPoint.time) / 1000f
    if (timeInSeconds <= 0) return 0f
    
    // Скорость в м/с
    val speedInMs = totalDistance / timeInSeconds
    // Конвертация в км/ч
    return speedInMs * 3.6f
}
```

## 📊 Производительность и оптимизация

### Оптимизация использования батареи

Приложение оптимизировано для минимизации расхода батареи:

1. **Настройки частоты обновления местоположения**:
   - Пользователь может выбрать частоту обновления в настройках
   - Более редкие обновления экономят заряд батареи

2. **Режимы точности**:
   - Высокая точность (GPS + сеть) - наиболее точный, но энергоемкий
   - Сбалансированный (преимущественно сеть) - средняя точность и энергопотребление
   - Экономичный (только сеть) - наименее точный, но экономит батарею

3. **Фильтрация обновлений местоположения**:
   - Игнорирование обновлений с низкой точностью
   - Минимальное расстояние между обновлениями для фильтрации шума

### Оптимизация использования памяти

Приложение оптимизировано для эффективного использования памяти:

1. **Управление жизненным циклом**:
   - Правильное освобождение ресурсов в методах жизненного цикла
   - Использование WeakReference для предотвращения утечек памяти

2. **Эффективное хранение данных**:
   - Использование примитивных типов вместо объектов где возможно
   - Оптимизация структур данных для минимизации дублирования

3. **Загрузка карт**:
   - Кэширование тайлов карты для уменьшения сетевого трафика
   - Загрузка только видимой области карты

### Оптимизация базы данных

Для эффективной работы с базой данных применяются следующие оптимизации:

1. **Индексирование**:
   - Создание индексов для часто запрашиваемых полей
   - Оптимизация запросов для быстрого поиска

2. **Пакетные операции**:
   - Использование транзакций для пакетной вставки точек маршрута
   - Минимизация количества операций с базой данных

3. **Асинхронные операции**:
   - Выполнение операций с базой данных в фоновом потоке
   - Использование Coroutines для асинхронных операций

## 🔒 Безопасность и разрешения

### Обработка разрешений

Приложение запрашивает и обрабатывает разрешения в соответствии с рекомендациями Android:

1. **Запрос разрешений**:
   - Разрешения запрашиваются при первом запуске приложения
   - Предоставляется объяснение, зачем нужны разрешения
   - Обрабатываются случаи отказа в разрешениях

2. **Проверка разрешений**:
   - Перед использованием функций, требующих разрешения, выполняется проверка
   - При отсутствии разрешения предлагается его предоставить

3. **Обработка изменений разрешений**:
   - Приложение корректно обрабатывает случаи, когда пользователь отзывает разрешения
   - Функциональность адаптируется к доступным разрешениям

### Хранение данных

Данные пользователя хранятся безопасно:

1. **Локальное хранение**:
   - Все данные хранятся локально на устройстве
   - Не передаются на внешние серверы без явного согласия пользователя

2. **Шифрование**:
   - Чувствительные данные могут быть зашифрованы
   - Используются стандартные механизмы шифрования Android

### Подробное описание работы с разрешениями

#### Запрос разрешений на доступ к местоположению

Приложение требует разрешения ACCESS_FINE_LOCATION для точного определения местоположения:

1. **Проверка разрешений**:
   ```kotlin
   private fun checkLocationPermission() {
       if (ContextCompat.checkSelfPermission(
               requireContext(),
               Manifest.permission.ACCESS_FINE_LOCATION
           ) == PackageManager.PERMISSION_GRANTED
       ) {
           locationPermissionGranted = true
           initStartLocationUpdates()
       } else {
           requestLocationPermission()
       }
   }
   ```

2. **Запрос разрешений**:
   ```kotlin
   private fun requestLocationPermission() {
       ActivityCompat.requestPermissions(
           requireActivity(),
           arrayOf(Manifest.permission.ACCESS_FINE_LOCATION),
           LOCATION_PERMISSION_REQUEST_CODE
       )
   }
   ```

3. **Обработка результата запроса**:
   ```kotlin
   override fun onRequestPermissionsResult(
       requestCode: Int,
       permissions: Array<out String>,
       grantResults: IntArray
   ) {
       if (requestCode == LOCATION_PERMISSION_REQUEST_CODE) {
           if (grantResults.isNotEmpty() && grantResults[0] == PackageManager.PERMISSION_GRANTED) {
               locationPermissionGranted = true
               initStartLocationUpdates()
           } else {
               // Показать сообщение о необходимости разрешения
           }
       }
   }
   ```

### Советы по расширению и модификации приложения

1. **Добавление новых функций**:
   - Экспорт маршрутов в GPX/KML формат
   - Статистика по маршрутам (графики, суммарные показатели)
   - Категоризация маршрутов (бег, велосипед, ходьба)
   - Социальные функции (поделиться маршрутом)

2. **Улучшение UI/UX**:
   - Темная тема
   - Анимации переходов между экранами
   - Виджеты для быстрого доступа к функциям
   - Поддержка планшетов с двухпанельным интерфейсом

3. **Технические улучшения**:
   - Миграция на Jetpack Compose для UI
   - Использование Hilt для внедрения зависимостей
   - Добавление модульных и инструментальных тестов
   - Оптимизация для работы на устройствах с ограниченными ресурсами